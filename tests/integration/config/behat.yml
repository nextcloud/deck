default:
  formatters:
    pretty:
      output_styles:
        comment: [ 'bright-blue' ]
  suites:
    test:
      paths:
        - '%paths.base%/../features/'
      filters:
        tags: '~@federation'
      contexts:
        - ServerContext:
            baseUrl:  http://localhost:8080/
        - RequestContext
        - BoardContext
        - CommentContext
        - AttachmentContext
        - SearchContext
        - SessionContext
    federation:
      paths:
        - '%paths.base%/../features/federation/'
      contexts:
        - FederationContext
