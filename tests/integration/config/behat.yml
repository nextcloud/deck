default:
  suites:
    test:
      paths:
        - '%paths.base%/../features/'
      contexts:
        - ServerContext:
            baseUrl:  http://localhost:8080/
        - RequestContext
        - BoardContext
        - CommentContext
        - SearchContext
