default:
  suites:
    test:
      paths:
        - '%paths.base%/../features/'
      contexts:
        - ServerContext:
            baseUrl:  http://localhost:9090/
        - RequestContext
        - BoardContext
        - CommentContext
        - SearchContext
